<?xml version="1.0" encoding="utf-8"?>
<project>
	
	<!-- Use the following for an Android Java extension, not needed otherwise -->
	<dependency name="natives" path="dependencies/android" if="android" />
	<android extension="com.gerantech.extension.Natives" />
	
</project>